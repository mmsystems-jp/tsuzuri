# 綴り（Tsuzuri）引継ぎ書
> 作成日: 2026-03-02 22:00

---

## 現在の状態

**Sign in with Apple 実装完了・EASビルド実行中（またはこれから実行）**

ビルド完了後、TestFlight → スクリーンショット撮影 → App Store審査申請 の流れ。

---

## 今セッションで完了したこと

1. `/profile` → `/diary/profile` バグ修正（aiProfileが取得できていなかった）
2. 通知ON/OFFトグル追加（設定画面 / PATCH /auth/settings）
3. 落款「なし」バグ修正（buildColophon関数）
4. テストデータを7日分に拡充（ペルソナに合わせた内容）
5. 開発テストメニュー削除（リリース用クリーンアップ）
6. handleLogout の setTab('chat') → setTab('home') バグ修正
7. プラン表示「Standard（テスト中）」→「Standard」
8. **Sign in with Apple 実装**（バックエンド + フロントエンド）
9. LINEログインボタンを非表示化（バックエンドは残存）

---

## Sign in with Appleの仕組み（重要）

### なぜ必要か
Appleのガイドライン4.8：「サードパーティログイン（LINE等）がある場合、Sign in with Appleも必須」

### 認証フロー
```
ユーザーがAppleサインインボタンをタップ
↓
iOS がApple IDで認証（Face ID/パスコード）
↓
identityToken（JWT）がアプリに返ってくる
↓
POST /auth/apple に identityToken を送信
↓
Lambda で apple-signin-auth ライブラリがトークン検証
↓
Appleの sub（ユーザー固有ID）でDynamoDB検索
↓
初回：新規ユーザー作成 / 2回目以降：既存ユーザー取得
↓
セッショントークン発行 → SecureStoreに保存 → ログイン完了
```

### 重要な注意点
- `identityToken` はAppleから**初回ログインのみ**フルで返ってくる
- `user`（Apple側のID）も**初回のみ**返ってくる。2回目以降はnull
- そのためDynamoDBのGSI（appleUserId-index）でsub検索している
- LINEログインはUIから消えているがバックエンドは生きている（既存ユーザー・LINE通知のため）

### DynamoDB の変更
- appleUserId-index GSI を作成（UPDATING → 数分でACTIVE）

---

## ファイル構成（変更があったもの）

| ファイル | 変更内容 |
|---|---|
| mobile/App.js | Sign in with Apple実装、LINEボタン削除、各種バグ修正 |
| mobile/app.json | ios.usesAppleSignIn: true追加、expo-apple-authentication追加 |
| backend/lambda/auth/index.ts | POST /auth/apple追加、PATCH /auth/settings追加 |
| backend/lambda/diary/index.ts | /diary/profile エンドポイント追加 |

---

## 次のアクション（優先順）

### 1. EASビルド実行（まだなら）
```powershell
cd C:\mmsystems\projects\tsuzuri\mobile
eas build --platform ios --profile production --non-interactive
```

### 2. TestFlight配信
```powershell
eas submit --platform ios --latest
```

### 3. TestFlightで動作確認
- Sign in with Apple でログインできるか
- FaceIDで再起動後にそのまま入れるか
- 開発テストメニューが消えているか
- チャット・日記保存・記録タブが動くか

### 4. スクリーンショット撮影（iPhone実機）
- 必要サイズ：6.9インチ（iPhone 16 Pro Max相当）必須
- 撮影画面：ホーム・チャット・日記プレビュー・記録タブ・設定
- 以前のスクリーンショットはUIが変わっているので撮り直し必須

### 5. App Store Connect で審査申請
- スクリーンショットアップロード
- 説明文・キーワード確認
- 年齢制限設定（17+推奨、AIチャット含むため）
- レビューメモ：「Sign in with Appleでのみログイン可能。LINEアカウント不要」と記載

---

## インフラ情報

| 項目 | 値 |
|---|---|
| API URL | https://o5k36gp6jd.execute-api.ap-northeast-1.amazonaws.com |
| DynamoDB | tsuzuri-prod-main |
| LINE Channel ID | 2009194648 |
| LIFF URL | https://liff.line.me/2009194648-zubANYib |
| LINE通知時刻 | 毎日21:00 JST（EventBridge: cron(0 12 * * ? *)） |
| EAS Project | mmsystems/cho |
| Bundle ID | jp.mmsystems.cho |
| Apple ID（App Store Connect） | 6759586476 |

---

## EASビルド残数
- Freeプラン：月15回まで（毎月1日リセット）
- 今月残り：3回程度（要確認）
- 超過時：Starter $19/月（$45クレジット≒22回分）に一時加入→戻す

---

## 既知の問題・注意点

- LINE通知はlineUserIdがある + notifyEnabled≠false + 今日の日記なし のユーザーのみ送信
- aiProfileはPOST/PUT /diary 時に非同期で抽出・更新される（保存直後は反映されないことがある）
- 追記時の落款二重防止：`/\n\n— .+$/` で末尾の落款を除去してから結合
- PowerShellでJSON引数が必要なAWS CLIは必ずファイル経由
