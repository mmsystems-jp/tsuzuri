# Tsuzuri 引継ぎ書 2026-02-28 23:59

## セッションでやったこと

### 完了
- ✅ 落款二重バグ修正（追記保存時に既存末尾の落款を正規表現で除去）
- ✅ 週カレンダーのスワイプ対応（PanResponder追加、左右スワイプで週移動）
- ✅ 落款設定UIの刷新（締め言葉3択ボタン＋名前入力フィールド）
- ✅ AI記憶機能実装（段階1＋段階2の併用）
  - GET /profile エンドポイント追加（aiProfile＋直近3日の日記を返す）
  - 日記保存後にプロフィール抽出（fire-and-forget）
  - App.js ChatScreen起動時にGET /profileを叩いてシステムプロンプトに注入
- ✅ callClaudeにAPIエラーチェック追加（`data.type === 'error'`でthrow）
- ✅ テストデータをペルソナ反映内容に変更（既存全削除＋3日分投入）
- ✅ バックエンドビルド＆デプロイ完了（tsuzuri-prod-diary 更新）

---

## 現在の実装状態

### 記憶機能の仕組み
1. 日記保存（POST/PUT）→ `extractAndUpdateProfile()` をfire-and-forgetで呼び出し
2. Claude Haikuで会話から職業・家族・趣味・性格・トピックを抽出
3. DynamoDB `PROFILE` アイテムの `aiProfile` フィールドにJSONで保存
4. チャット起動時に `GET /profile` → aiProfile＋直近3日の日記を取得
5. `buildSystemPrompt(questionCount, profile, recentDiaries)` に注入

### テスト状況
- アプリ起動：✅
- チャット動作：✅（Claude APIエラーチェック追加後に正常動作確認）
- 記憶機能：テスト用ペルソナデータ投入済み、プロフィール抽出の動作確認は次セッション

### ペルソナデータ内容（テスト用）
- 職業：SIerのインフラエンジニア・マネージャー（15年）、副業でReact Native開発
- 家族：妻・息子あり
- 趣味・興味：副業アプリ開発、AWS、AI
- よく話すテーマ：副業、開発、仕事とのバランス

---

## テスト項目（次セッションで実施）

### AI記憶機能テスト
- [ ] 設定→テストデータ作成でペルソナ3日分が投入される
- [ ] 30秒待ってからチャットを開く（プロフィール抽出が走る時間）
- [ ] AIが「エンジニア」「副業」「息子」などを参照した質問をする

### 通常機能テスト
- [ ] HomeScreen：今日の記録なし → 挨拶文＋「話し始める」
- [ ] HomeScreen：今日の記録あり → 続きを読む/書く/見るだけ
- [ ] チャット：2回話した後にcanGenerate有効化
- [ ] チャット：右上ボタンが茶色塗りつぶし＋パルスアニメーション
- [ ] チャット：AIの「はい」「いいえ」判定
- [ ] プレビュー：「続きを話す」「最初から話す」
- [ ] プレビュー：保存後に記録タブへ遷移
- [ ] 記録タブ：週カレンダーのスワイプ動作
- [ ] 記録タブ：記録のある日は太字＋ドット
- [ ] 設定：落款の締め言葉選択＋名前入力
- [ ] 設定：落款付き日記保存確認

---

## 技術情報

### パス
- **App.js**: `C:\mmsystems\projects\tsuzuri\mobile\App.js`
- **diary Lambda**: `C:\mmsystems\projects\tsuzuri\backend\lambda\diary\index.ts`
- **引継ぎ書（最新）**: `C:\mmsystems\projects\tsuzuri\HANDOVER_20260228_2359.md`

### 定数
- LINE_CHANNEL_ID: 2009194648
- API_URL: https://o5k36gp6jd.execute-api.ap-northeast-1.amazonaws.com
- canGenerate条件: `userTurnCount >= 2`（固定）

### EASビルド
- 今月残り: 1回
- 3/1リセット → 15回利用可能
- Expo Goで確認してからビルド

---

## 作業完了後に必ずやること

`C:\mmsystems\project_status.json` の `apps` 配列内、`"name": "綴り / Tsuzuri"` のエントリを実態に合わせて更新する。

```json
"progress": 実態に合わせた数値,
"status":   "現在のステータス",
"done":     ["完了済みタスクを追加"],
"todo":     ["残りタスク（完了したものは削除）"],
"next":     "次のアクション"
```

EAS Build を消費した場合は `eas_remaining` も減らす。

---

## 次セッションの進め方

1. PROJECTS.md と本引継ぎ書を読む
2. 設定→テストデータ作成でペルソナ投入
3. 30秒待ってからチャット開始 → AIの質問にペルソナが反映されているか確認
4. 通常機能テストを上から順に実施
5. 全項目✅になったらApp Store申請に向けたEASビルド
